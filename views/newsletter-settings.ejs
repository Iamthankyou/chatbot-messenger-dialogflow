<!DOCTYPE html>
<html>
<head>
    <title>Cài đặt cá nhân</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>

    <link rel="stylesheet" href="/webview.css" />
</head>
<body>
<div class="container">
    <h3>Cài đặt nhận tin đăng ký</h3>
    <form action="/webviews/save" method="get">
        <input type="hidden" name="psid" id="psid">
        <h5>Tần suất</h5>
        <input type="radio" name="newsletter" value="1"> Theo tuần<br/>
        <input type="radio" name="newsletter" value="2"> Theo ngày<br/>
        <input type="radio" name="newsletter" value="0"> Không bao giờ<br/>

        <h5>Chủ đề</h5>
        <input type="checkbox" name="topics[]" value="IPhone">Đồ chơi<br/>
        <input type="checkbox" name="topics[]" value="IPad">Dụng cụ<br/>

        <h5>Gửi khuyến mại</h5>
        <input type="radio" name="deals" value="1">Có<br/>
        <input type="radio" name="deals" value="0">Không<br/>

        <button id="submitButton" type="submit">Xác nhận</button>
    </form>
</div>

<script>
    (function(d, s, id){
        var js, fjs = d.getElementsByTagName(s)[0];
        if (d.getElementById(id)) {return;}
        js = d.createElement(s); js.id = id;
        js.src = "//connect.facebook.com/en_US/messenger.Extensions.js";
        fjs.parentNode.insertBefore(js, fjs);
    }(document, 'script', 'Messenger'));
    window.extAsyncInit = function() {
        // the Messenger Exstensions JS SDK is done loading
        document.getElementById('submitButton').addEventListener('click', function() {
            MessengerExtensions.requestCloseBrowser(function success() {
                // webview closed
            }, function error(err) {
                // an error occured
            });
        });

        // APP_ID
        MessengerExtensions.getContext('252327929863548',
            function success(thread_context){
                document.getElementById('psid').value = thread_context.psid;
            },
            function error(err){
                // error
            }
        );
    };
</script>
</body>
</html>